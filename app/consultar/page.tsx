'use client';
import { FormEvent,useState } from 'react';
import { useRouter } from 'next/navigation';
import { BrandLogo } from '@/components/BrandLogo';
import { CarFront,Search,ShieldCheck } from 'lucide-react';
export default function ConsultarPage(){const router=useRouter();const [plate,setPlate]=useState('');const [phone,setPhone]=useState('');const [loading,setLoading]=useState(false);const [error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/public/lookup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({plate,phone})});const d=await r.json();setLoading(false);if(!r.ok||!d.found){setError(d.error||'Nenhuma permanência ativa encontrada com esses dados.');return;}router.push(`/cliente/${d.token}`)}return <main className="min-h-screen bg-gradient-to-b from-brand-50 to-white p-4"><div className="mx-auto max-w-md pt-8"><BrandLogo/><div className="mt-8 rounded-[2rem] bg-white p-6 shadow-soft"><div className="rounded-2xl bg-brand-50 p-3 text-brand-600 w-fit"><CarFront/></div><h1 className="mt-4 text-3xl font-black">Consultar estacionamento</h1><p className="mt-2 text-slate-500">Veja tempo e valor atual sem precisar do QR.</p><form onSubmit={submit} className="mt-6 space-y-4"><label className="block text-sm font-bold">Placa<input value={plate} onChange={e=>setPlate(e.target.value.toUpperCase())} required placeholder="ABC1D23" className="mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3 text-lg font-black uppercase outline-none focus:border-brand-500"/></label><label className="block text-sm font-bold">WhatsApp cadastrado<input value={phone} onChange={e=>setPhone(e.target.value.replace(/\D/g,'').slice(0,13))} required inputMode="numeric" maxLength={13} placeholder="71999999999" className="mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3 text-lg font-black outline-none focus:border-brand-500"/></label>{error&&<p className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-2xl bg-brand-600 px-4 py-4 font-black text-white disabled:opacity-50"><Search size={18}/>{loading?'Consultando...':'Consultar'}</button></form><div className="mt-5 flex gap-2 rounded-2xl bg-slate-50 p-4 text-xs text-slate-500"><ShieldCheck size={18} className="shrink-0 text-brand-600"/>O WhatsApp completo é validado para evitar que apenas conhecer uma placa seja suficiente para acessar a permanência.</div></div></div></main>}
