'use client';import { FormEvent,useEffect,useState } from 'react';import { AppShell } from '@/components/AppShell';import { AdminNav } from '@/components/AdminNav';
export default function Page(){const [rows,setRows]=useState<any[]>([]);const [error,setError]=useState('');const load=()=>fetch('/api/services').then(r=>r.json()).then(d=>setRows(d.services||[]));useEffect(()=>{void load()},[]);async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/services',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),price:f.get('price'),active:true})});const d=await r.json();if(!r.ok){setError(d.error);return}(e.currentTarget as HTMLFormElement).reset();load()}async function toggle(x:any){await fetch('/api/services',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:x.id,active:!x.active})});load()}return <AppShell title="Gestão"><AdminNav/><h1 className="text-3xl font-black">Serviços</h1><p className="mt-2 text-slate-500">Lavagem, valet, recarga e outros adicionais.</p><form onSubmit={create} className="mt-6 grid gap-3 rounded-3xl bg-white p-5 shadow-sm sm:grid-cols-[1fr_180px_auto]"><input name="name" required placeholder="Nome do serviço" className="rounded-2xl border border-slate-200 px-4 py-3"/><input name="price" required type="number" step="0.01" min="0" placeholder="Valor" className="rounded-2xl border border-slate-200 px-4 py-3"/><button className="rounded-2xl bg-brand-600 px-5 font-bold text-white">Cadastrar</button>{error&&<p className="text-sm text-red-600 sm:col-span-3">{error}</p>}</form><div className="mt-6 grid gap-3 md:grid-cols-2">{rows.map(x=><div key={x.id} className="flex items-center justify-between rounded-3xl bg-white p-5 shadow-sm"><div><h2 className="font-black">{x.name}</h2><p className="text-sm text-slate-500">R$ {Number(x.price).toFixed(2).replace('.',',')}</p></div><button onClick={()=>toggle(x)} className={`rounded-full px-3 py-2 text-xs font-black ${x.active?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-500'}`}>{x.active?'Ativo':'Inativo'}</button></div>)}</div></AppShell>}
