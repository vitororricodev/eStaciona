import { NextRequest,NextResponse } from 'next/server';import { getContext,canManage } from '@/lib/authz';
export async function GET(req:NextRequest){const {supabase,profile}=await getContext();if(!profile||!canManage(profile.role))return NextResponse.json({error:'Sem permissão'},{status:403});const limit=Math.min(500,Math.max(20,Number(req.nextUrl.searchParams.get('limit')||200)));const {data,error}=await supabase.from('audit_logs').select('id,actor_user_id,action,entity,entity_id,metadata,created_at').eq('organization_id',profile.organization_id).order('created_at',{ascending:false}).limit(limit);return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({logs:data||[]});}
