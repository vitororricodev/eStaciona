import { NextRequest, NextResponse } from 'next/server';
import { getContext, canManage } from '@/lib/authz';
import { z } from 'zod';
const schema=z.object({name:z.string().trim().min(2).max(80),price:z.coerce.number().min(0).max(999999),active:z.boolean().optional().default(true)});
export async function GET(){const {supabase,profile}=await getContext();if(!profile)return NextResponse.json({error:'Perfil não configurado'},{status:403});const {data,error}=await supabase.from('services').select('*').eq('organization_id',profile.organization_id).order('active',{ascending:false}).order('name');return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({services:data||[]});}
export async function POST(req:NextRequest){const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:'Dados inválidos'},{status:400});const {supabase,user,profile}=await getContext();if(!profile||!canManage(profile.role))return NextResponse.json({error:'Sem permissão'},{status:403});const {data,error}=await supabase.from('services').insert({...p.data,organization_id:profile.organization_id}).select().single();if(error)return NextResponse.json({error:error.message},{status:500});await supabase.from('audit_logs').insert({organization_id:profile.organization_id,actor_user_id:user?.id,action:'service.created',entity:'service',entity_id:data.id,metadata:{name:data.name,price:data.price}});return NextResponse.json({service:data},{status:201});}
export async function PATCH(req:NextRequest){const body=await req.json();const id=String(body.id||'');const p=schema.partial().safeParse(body);if(!id||!p.success)return NextResponse.json({error:'Dados inválidos'},{status:400});const {supabase,user,profile}=await getContext();if(!profile||!canManage(profile.role))return NextResponse.json({error:'Sem permissão'},{status:403});const payload:any={...p.data};delete payload.id;const {data,error}=await supabase.from('services').update(payload).eq('id',id).eq('organization_id',profile.organization_id).select().single();if(error)return NextResponse.json({error:error.message},{status:500});await supabase.from('audit_logs').insert({organization_id:profile.organization_id,actor_user_id:user?.id,action:'service.updated',entity:'service',entity_id:id,metadata:{fields:Object.keys(payload)}});return NextResponse.json({service:data});}
