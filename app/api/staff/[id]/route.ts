import { NextRequest,NextResponse } from 'next/server';
import { getContext,isOwner } from '@/lib/authz';
import { z } from 'zod';
const schema=z.object({role:z.enum(['manager','operator']).optional(),active:z.boolean().optional(),name:z.string().min(2).max(80).optional()});
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){const {id}=await params;const p=schema.safeParse(await req.json());if(!p.success)return NextResponse.json({error:'Dados inválidos'},{status:400});const {supabase,user,profile}=await getContext();if(!profile||!isOwner(profile.role))return NextResponse.json({error:'Apenas proprietário pode alterar equipe.'},{status:403});if(id===user?.id&&p.data.active===false)return NextResponse.json({error:'Você não pode desativar seu próprio usuário.'},{status:409});const {data,error}=await supabase.from('profiles').update(p.data).eq('id',id).eq('organization_id',profile.organization_id).select().single();if(error)return NextResponse.json({error:error.message},{status:500});await supabase.from('audit_logs').insert({organization_id:profile.organization_id,actor_user_id:user?.id,action:'staff.updated',entity:'profile',entity_id:id,metadata:p.data});return NextResponse.json({staff:data});}
