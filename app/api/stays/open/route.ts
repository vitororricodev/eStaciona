import { NextResponse } from 'next/server';
import { createClient } from '@/lib/supabase/server';
import { stayTotals } from '@/lib/stayTotals';
export async function GET(){const supabase=await createClient();const {data:profile}=await supabase.from('profiles').select('organization_id').single();if(!profile)return NextResponse.json({error:'Perfil não configurado'},{status:403});const {data,error}=await supabase.from('stays').select('id,public_token,started_at,status,paid_at,paid_amount,payment_grace_until,vehicles(id,plate,make,model,color,customers(id,name,phone)),tariff_plans(*),stay_services(id,service_name,unit_price,quantity)').eq('organization_id',profile.organization_id).eq('status','open').order('started_at',{ascending:true}).limit(200);if(error)return NextResponse.json({error:error.message},{status:500});const rows=(data||[]).filter((s:any)=>s.vehicles).map((s:any)=>{const totals=stayTotals(s);return {...s,tariff_plans:totals.tariff,...totals};});return NextResponse.json({stays:rows});}
