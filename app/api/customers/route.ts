import { NextRequest,NextResponse } from 'next/server';
import { getContext,canManage } from '@/lib/authz';
export async function GET(req:NextRequest){const {supabase,profile}=await getContext();if(!profile||!canManage(profile.role))return NextResponse.json({error:'Sem permissão'},{status:403});const q=String(req.nextUrl.searchParams.get('q')||'').trim();let query=supabase.from('customers').select('id,name,phone,cpf,notes,active,created_at,vehicles(id,plate,make,model,color,year,active,stays(id,status,started_at,ended_at,final_amount))').eq('organization_id',profile.organization_id).order('created_at',{ascending:false}).limit(300);if(q)query=query.or(`name.ilike.%${q}%,phone.ilike.%${q}%`);const {data,error}=await query;return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({customers:data||[]});}
